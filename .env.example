# Database Configuration
# Do not store database credentials in this repo.
# Railway should provide DATABASE_URL and DATABASE_URL_TARIFF as environment variables.
DATABASE_URL=
DATABASE_URL_TARIFF=

# Node Environment
NODE_ENV=production
