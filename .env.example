# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:port/database
DATABASE_URL_TARIFF=postgresql://username:password@hostname:port/tariff_database

# Alternative format for Railway (use the specific variables)
PGDATABASE=your_database_name
PGHOST=your_host
PGPASSWORD=your_password
PGPORT=your_port
PGUSER=your_username

# Node Environment
NODE_ENV=production
