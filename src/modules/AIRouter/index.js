/**
 * AI Router Module - Main Entry Point
 * 
 * This file provides a clean import interface for the AI Router module.
 * 
 * Usage:
 *   const { aiRouter } = require('./src/modules/AIRouter');
 *   // or
 *   const { aiRouter } = require('./src/modules/AIRouter/index');
 * 
 * Re-exports everything from aiRouter.js for convenience.
 */

module.exports = require('./aiRouter');
